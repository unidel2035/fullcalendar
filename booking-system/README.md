# Система бронирования для посуточной аренды

**Daily Rental Booking System** - Полнофункциональная система управления бронированиями для объектов посуточной аренды с интеграцией FullCalendar и базы данных Integram.

## 🌟 Основные возможности

### Управление бронированиями
- ✅ Создание, просмотр, редактирование и отмена бронирований
- ✅ Проверка доступности объектов в реальном времени
- ✅ Визуализация бронирований через FullCalendar
- ✅ Автоматический расчет стоимости с учетом всех факторов
- ✅ Детальная разбивка цены по позициям

### Динамическое ценообразование
- 💰 Базовая цена за ночь
- 💰 Наценки за выходные дни
- 💰 Сезонные корректировки цен
- 💰 Скидки за длительное проживание
- 💰 Специальные тарифы (early bird, last minute)
- 💰 Гибкая система правил ценообразования

### Ограничения и правила бронирования
- 🔒 Минимальная/максимальная длительность проживания
- 🔒 Блокировка дат (blackout dates)
- 🔒 Ограничение количества гостей
- 🔒 Правила заезда/выезда по дням недели
- 🔒 Ограничения на advance booking
- 🔒 Поддержка нескольких объектов одновременно

### Регистрация гостей
- 👤 Полная информация о гостях (ФИО, контакты, паспортные данные)
- 👤 История бронирований
- 👤 Интеграция с Telegram (опционально)
- 👤 Черный список гостей

### Логирование и аудит
- 📝 Детальный аудит всех изменений бронирований
- 📝 Системные логи (файлы + база данных)
- 📝 Логирование действий пользователей
- 📝 История платежей

### Интеграция с Integram
- 🔄 Синхронизация с базой данных Integram
- 🔄 Поддержка MCP сервера
- 🔄 Telegram уведомления
- 🔄 Единая система пользователей

## 📋 Требования

- PHP 7.4 или выше (рекомендуется PHP 8.0+)
- MySQL 5.7+ или MariaDB 10.3+
- Web-сервер (Apache/Nginx)
- Composer (опционально, для зависимостей)

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/unidel2035/fullcalendar.git
cd fullcalendar/booking-system
```

### 2. Настройка базы данных

```bash
# Создайте базу данных
mysql -u root -p
CREATE DATABASE booking_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'booking_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON booking_system.* TO 'booking_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# Импортируйте схему
mysql -u booking_user -p booking_system < database/schema.sql
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корневой директории:

```bash
# Database Configuration
DB_HOST=localhost
DB_NAME=booking_system
DB_USER=booking_user
DB_PASS=your_password

# Integram Configuration (опционально)
INTEGRAM_DB_HOST=localhost
INTEGRAM_DB_NAME=integram
INTEGRAM_DB_USER=integram_user
INTEGRAM_DB_PASS=integram_password
INTEGRAM_SYNC_ENABLED=false

# Application Settings
APP_DEBUG=true
APP_TIMEZONE=Europe/Moscow
LOG_LEVEL=info

# Security
JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING_IN_PRODUCTION

# CORS (если фронтенд на другом домене)
CORS_ALLOWED_ORIGINS=*

# Telegram Bot (опционально)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ENABLED=false

# Integram API (опционально)
INTEGRAM_API_URL=https://dev.drondoc.ru/integram
INTEGRAM_API_TOKEN=
```

### 4. Настройка web-сервера

#### Apache (.htaccess)

```apache
RewriteEngine On
RewriteBase /booking-system/api/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
```

#### Nginx

```nginx
location /booking-system/api/ {
    try_files $uri $uri/ /booking-system/api/index.php?$query_string;
}
```

### 5. Установка зависимостей (опционально)

Если вы используете Composer:

```bash
cd api
composer install
```

### 6. Настройка прав доступа

```bash
chmod -R 755 booking-system/
chmod -R 777 booking-system/logs/
chmod -R 777 booking-system/uploads/
```

## 📖 Использование

### API Endpoints

#### Бронирования

```bash
# Список всех бронирований
GET /api/v1/bookings/list?page=1&page_size=20

# Фильтрация бронирований
GET /api/v1/bookings/list?property_id=1&status=confirmed&check_in_from=2025-01-01

# Получить бронирование по ID
GET /api/v1/bookings/123

# Создать новое бронирование
POST /api/v1/bookings/list
{
  "property_id": 1,
  "guest_id": 5,
  "check_in": "2025-01-15",
  "check_out": "2025-01-17",
  "number_of_guests": 2,
  "number_of_adults": 2,
  "number_of_children": 0,
  "special_requests": "Ранний заезд"
}

# Обновить бронирование
PUT /api/v1/bookings/123
{
  "status": "confirmed",
  "payment_status": "paid"
}

# Отменить бронирование
POST /api/v1/bookings/cancel/123
{
  "reason": "Изменение планов гостя"
}

# Проверить доступность
POST /api/v1/bookings/check-availability
{
  "property_id": 1,
  "check_in": "2025-01-15",
  "check_out": "2025-01-17"
}

# Календарь доступности
GET /api/v1/bookings/availability/1?start_date=2025-01-01&end_date=2025-01-31
```

#### Гости

```bash
# Список гостей
GET /api/v1/guests/list?page=1&search=Иванов

# Получить гостя по ID
GET /api/v1/guests/5

# Создать гостя
POST /api/v1/guests/list
{
  "first_name": "Иван",
  "last_name": "Иванов",
  "middle_name": "Иванович",
  "email": "ivanov@example.com",
  "phone": "+79001234567",
  "date_of_birth": "1990-01-15",
  "passport_series": "1234",
  "passport_number": "567890"
}

# Обновить гостя
PUT /api/v1/guests/5
{
  "phone": "+79007654321",
  "email": "new_email@example.com"
}
```

#### Объекты недвижимости

```bash
# Список объектов
GET /api/v1/properties/list

# Получить объект по ID
GET /api/v1/properties/1

# Создать объект
POST /api/v1/properties/list
{
  "name": "Уютная квартира в центре",
  "address": "г. Москва, ул. Тверская, д. 1",
  "property_type": "apartment",
  "max_guests": 4,
  "bedrooms": 2,
  "bathrooms": 1,
  "square_meters": 65.5,
  "amenities": ["wifi", "kitchen", "washing_machine", "parking"],
  "is_active": true
}
```

### Использование фронтенда

1. Откройте `examples/index.html` в браузере
2. Выберите объект недвижимости из списка
3. Кликните на дату для создания нового бронирования
4. Заполните форму бронирования
5. Нажмите "Рассчитать цену" для расчета стоимости
6. Нажмите "Создать бронирование" для подтверждения

## 🏗️ Структура проекта

```
booking-system/
├── api/
│   ├── index.php                 # Главный роутер API
│   ├── config.php                # Конфигурация
│   ├── Database.php              # Менеджер подключений к БД
│   ├── Logger.php                # Система логирования
│   ├── BookingController.php     # Контроллер бронирований
│   ├── GuestController.php       # Контроллер гостей
│   ├── PropertyController.php    # Контроллер объектов
│   ├── PricingEngine.php         # Движок ценообразования
│   └── BookingValidator.php      # Валидация данных
├── database/
│   └── schema.sql                # Схема базы данных
├── examples/
│   └── index.html                # Пример интерфейса с FullCalendar
├── logs/                         # Логи приложения
├── uploads/                      # Загруженные файлы
├── .env.example                  # Пример переменных окружения
└── README.md                     # Документация
```

## 🗄️ Структура базы данных

### Основные таблицы

- **properties** - Объекты недвижимости
- **guests** - Информация о гостях
- **bookings** - Бронирования
- **pricing_rules** - Правила ценообразования
- **booking_restrictions** - Ограничения на бронирование
- **booking_price_breakdown** - Детализация цены
- **payments** - Платежи
- **booking_audit_log** - Аудит изменений
- **system_log** - Системные логи
- **users** - Пользователи системы
- **telegram_notifications** - Очередь Telegram уведомлений
- **integram_sync** - Синхронизация с Integram

### Представления (Views)

- **active_bookings** - Текущие и будущие бронирования
- **property_availability** - Доступность объектов
- **booking_revenue_summary** - Аналитика выручки

## 💡 Примеры использования

### Добавление правила выходных дней

```sql
INSERT INTO pricing_rules
(property_id, rule_name, rule_type, priority, adjustment_type, adjustment_value, days_of_week, is_active)
VALUES
(1, 'Наценка выходные', 'weekend', 10, 'percentage', 20.00, 'saturday,sunday', TRUE);
```

### Добавление минимального срока проживания

```sql
INSERT INTO booking_restrictions
(property_id, restriction_type, restriction_name, int_value, is_active)
VALUES
(1, 'min_stay', 'Минимум 2 ночи', 2, TRUE);
```

### Блокировка дат

```sql
INSERT INTO booking_restrictions
(property_id, restriction_type, restriction_name, start_date, end_date, is_active, notes)
VALUES
(1, 'blackout', 'Ремонт', '2025-02-01', '2025-02-07', TRUE, 'Плановый ремонт квартиры');
```

## 🔧 Настройка и расширение

### Добавление нового типа правила ценообразования

1. Добавьте новый тип в enum `rule_type` таблицы `pricing_rules`
2. Обновите логику в `PricingEngine.php`
3. Добавьте обработку в методе `calculateBookingPrice()`

### Интеграция с платежными системами

Расширьте таблицу `payments` и добавьте обработчики в `PaymentController.php`:

```php
class PaymentController {
    public function processPayment($bookingId, $amount, $paymentMethod) {
        // Интеграция с платежным провайдером
    }
}
```

### Telegram уведомления

Активируйте Telegram бота в `.env`:

```
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_ENABLED=true
```

Создайте `TelegramNotifier.php` для отправки уведомлений.

## 📊 Мониторинг и обслуживание

### Очистка старых логов

```sql
-- Удалить логи старше 90 дней
DELETE FROM system_log WHERE timestamp < DATE_SUB(NOW(), INTERVAL 90 DAY);
DELETE FROM booking_audit_log WHERE timestamp < DATE_SUB(NOW(), INTERVAL 365 DAY);
```

### Резервное копирование

```bash
# Создать бэкап базы данных
mysqldump -u booking_user -p booking_system > backup_$(date +%Y%m%d).sql

# Восстановление
mysql -u booking_user -p booking_system < backup_20250115.sql
```

## 🐛 Отладка

### Включение режима отладки

```php
// В .env
APP_DEBUG=true
LOG_LEVEL=debug
```

### Просмотр логов

```bash
# Последние системные логи
tail -f logs/$(date +%Y-%m-%d).log

# Логи из базы данных
SELECT * FROM system_log ORDER BY timestamp DESC LIMIT 100;
```

## 🔐 Безопасность

- ✅ Параметризованные запросы (защита от SQL injection)
- ✅ Валидация всех входных данных
- ✅ CORS настройки
- ✅ Аудит всех действий
- ✅ Хеширование паролей (bcrypt)
- ✅ JWT токены для авторизации
- ⚠️ **ВАЖНО**: Измените `JWT_SECRET` в production окружении!

## 📝 Лицензия

MIT License

## 👥 Авторы

Разработано для проекта FullCalendar / Integram

## 🤝 Поддержка

- GitHub Issues: https://github.com/unidel2035/fullcalendar/issues
- Документация Integram: https://dev.drondoc.ru/integram

## 🗺️ Roadmap

- [ ] Интеграция с внешними календарями (Google Calendar, Airbnb)
- [ ] Мобильное приложение
- [ ] Автоматические email/SMS уведомления
- [ ] Онлайн платежи
- [ ] Многопользовательские права доступа
- [ ] Экспорт отчетов (PDF, Excel)
- [ ] API документация (Swagger/OpenAPI)
- [ ] Unit тесты
- [ ] Docker контейнеризация

---

**Версия:** 1.0.0
**Дата обновления:** 2025-01-15
