# ================================================
# BOOKING SYSTEM CONFIGURATION
# ================================================

# Database Configuration
DB_HOST=localhost
DB_NAME=booking_system
DB_USER=booking_user
DB_PASS=your_secure_password_here

# Integram Database Configuration (optional)
INTEGRAM_DB_HOST=localhost
INTEGRAM_DB_NAME=integram
INTEGRAM_DB_USER=integram_user
INTEGRAM_DB_PASS=integram_password
INTEGRAM_SYNC_ENABLED=false

# Application Settings
APP_DEBUG=true
APP_TIMEZONE=Europe/Moscow
LOG_LEVEL=info

# Security Settings
# IMPORTANT: Change this to a random string in production!
JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING_IN_PRODUCTION_ENVIRONMENT
BCRYPT_COST=12

# CORS Settings (comma-separated list of allowed origins, or * for all)
CORS_ALLOWED_ORIGINS=*

# Telegram Bot Integration (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ENABLED=false

# Integram API Integration (optional)
INTEGRAM_API_URL=https://dev.drondoc.ru/integram
INTEGRAM_API_TOKEN=
INTEGRAM_SYNC_ENABLED=false

# File Upload Settings
UPLOAD_MAX_SIZE_MB=10
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,pdf,doc,docx

# Email Configuration (optional - for future use)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASS=
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Booking System

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Booking Defaults
DEFAULT_MIN_STAY_NIGHTS=1
DEFAULT_MAX_STAY_NIGHTS=30
DEFAULT_MAX_ADVANCE_DAYS=365
DEFAULT_MIN_ADVANCE_HOURS=2

# Currency
DEFAULT_CURRENCY=RUB
CURRENCY_SYMBOL=₽
